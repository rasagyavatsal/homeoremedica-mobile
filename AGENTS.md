# Repository instructions

Use Conventional Commits. Never bundle the private production remedies database.

After code changes run `npm run android`, `npm test`, and `npm run typecheck`, including a small-phone Android simulator for UI changes. Use test-driven development for functional behavior.
